# Suggested Commands

No code exists yet. Once scaffolded:

```bash
# Run the service
go run ./cmd/api/main.go

# Build
go build -o bin/youtube-watcher ./cmd/api/

# Test
go test ./...

# Lint (if golangci-lint installed)
golangci-lint run ./...

# Format
go fmt ./...
```
